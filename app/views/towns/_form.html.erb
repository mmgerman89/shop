<div class="container-fluid">
  <div class="row">
    <div class="panel-body">
      <%= form_for(town) do |f| %>
        <% if town.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(town.errors.count, "error") %> prohibited this town from being saved:</h2>

            <ul>
            <% town.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
            </ul>
          </div>
        <% end %>

        <div class="form-group">
          <%= f.text_field :name, autofocus: true, placeholder: "Nombre", class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.number_field :code, autofocus: true, placeholder: "Código Postal", class: "form-control" %>
        </div>

        <%= f.submit "Aceptar", class: "btn btn-success" %>
      <% end %>
    </div>
  </div>
</div>
